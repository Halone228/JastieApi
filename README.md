### Post setup
`pip install poetry`
### Setup
```shell
poetry install
```
### Run development
`poetry run dev`
### Run production
`poetry run production`

## Env params
For development, can just copy `.env.example`
